CREATE TABLE salon
(
  id INT NOT NULL AUTO_INCREMENT,
  name VARCHAR(100),
  location VARCHAR(100),
  website_link VARCHAR(100),
  email VARCHAR(100),
  contact_no VARCHAR(20),
  PRIMARY KEY (id)
);
